# Dream Report · YYYY-MM-DD

> 自动化记忆巩固报告（暂存区）。本报告只提案、不执行；人工闸确认后才允许固化。
> 状态：`staged` · 运行方式：`scheduled | manual` · 模型：`<model>`

## 0 · 运行元数据

- 触发与门控：`<gate 阈值 + 实际信号值 + pass>`
- 上期报告：`<路径 | 无>`（间隔 `<hours>` 小时）
- 本次扫描：prompts `<N>` 条 · memories `<N>` 条 · 护栏日志 `<N>` 行
- 运行锁：acquired `<时间>` → released `<时间>`
- 异常/跳过阶段：`<无 | 说明>`

## 1 · Orient 概览

- 记忆总量 `<N>`（项目分布摘要：top 3-5）
- 自上次报告新增：sessions `<N>` · prompts `<N>` · memories `<N>`
- 预算体量：AGENTS.md `<bytes/B>` / `<lines>L` · 技能 `<N>` 个 · 数据 `<KB>`
- 上期提案跟进：`<逐条 | 无上期>`

## 2 · Gather 信号

> 每条必须带来源引用；hedge 原样保留。

### 2.1 用户纠正（outcome:corrected 候选）

- `[prompt <id> · <日期>]` <纠正内容摘要；原文关键词原样保留>

### 2.2 显式偏好/长期约定

- `[prompt <id> · <日期>]` <内容>

### 2.3 反复出现的主题

- <主题>（出现 <N> 次：<prompt id 列表>）

### 2.4 重复成功路径（L2 证据）

- <路径描述>（证据：<prompt/memory id ×2+>）

### 2.5 护栏命中

- `[guardrail.log · <日期>]` <事件>

### 2.6 低置信观察

- <观察 | confidence: low>

## 3 · Consolidate 提案

### 3.1 近重合并候选

| 候选组 | memory ids | 建议合并稿 | 理由 |
|---|---|---|---|
| A | `mem_x`, `mem_y` | <合并稿全文> | <余弦带内近似 + 主题一致> |

### 3.2 冲突软失效候选

- `mem_x` vs `mem_y`：<冲突点> → 建议保留 `<id>` 有效 / `<id>` 软失效（valid_until `<date>`）

### 3.3 失效引用

- `mem_x`：指向 `<已消失的路径/决策>` → 建议 <处理方式>

### 3.4 教训与成功路径（带引用）

- <教训/路径全文>（来源：<ids>）

## 4 · Prune 提案

### 4.1 冷/陈旧候选（删除候选，需人工确认）

- `mem_x`（<创建日期>，<理由>）

### 4.2 预算观察

- <观察>

## 5 · L2 / L3 进化候选

### 5.1 技能候选（≥2 次成功证据）

- <技能草案：做什么 / 验证方式 / 证据>

### 5.2 规则候选（失败簇 ≥2 次）

- <规则草案 / 失败证据 / 影响面 / confidence>

## 6 · 下期建议

- <下一次 Dream 应重点检查什么>
